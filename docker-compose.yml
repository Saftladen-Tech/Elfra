version: '3.8'

services:
  nuxt-app:
    build: .
    image: Elfra:${IMAGE_VERSION}
    ports:
      - "3000:3000"
    volumes:
      - ./content/courses:/app/content/courses
      - ./customconfig:/app/customconfig
    environment:
      NODE_ENV: production
    restart: unless-stopped

