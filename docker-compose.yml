services:
  elfra:
    build: .
    image: elfra:1.0.0
    ports:
      - "3000:3000"
    volumes:
      - ./courses:/app/content/courses
      - ./customconfig:/app/customconfig
    environment:
      NODE_ENV: production
    restart: unless-stopped
