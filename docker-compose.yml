services:
  elfra:
    build: .
    image: elfra:1.0.0
    ports:
      - "3000:3000"
    volumes:
      - ./elfra-container/courses:/app/content/courses
      - ./elfra-container/config:/app/customconfig
    environment:
      NODE_ENV: production
    restart: unless-stopped
