services:
  nuxt-app:
    build: .
    image: elfra:0.0.1-alpha
    ports:
      - "3000:3000"
    volumes:
      - ./courses:/app/content/courses
      - ./customconfig:/app/customconfig
    environment:
      NODE_ENV: production
    restart: unless-stopped
